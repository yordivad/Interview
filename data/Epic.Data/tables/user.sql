CREATE TABLE [dbo].[User]
(
	[Id] BIGINT NOT NULL ,
	Name VARCHAR(200),
	LastName VARCHAR(200),
	Email VARCHAR(200) NOT NULL UNIQUE CLUSTERED,
	Password VARCHAR(500) NOT NULL,
	CONSTRAINT [PK_User] PRIMARY KEY  ([Id] ASC),

)
